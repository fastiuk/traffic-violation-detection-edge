# Core dependencies
numpy==1.26.4
opencv-python==4.11.0.86
flask

# CPU Inference
onnxruntime

# Note: Hailo bindings (hailo_platform) must be installed manually using the wheel provided in setup/installers/